## 动态版 swoole-cli 支持的包格式

.deb ，.rpm ，.apk（Alpine Package Keeper‌） ，.ipk ，Flatpak ，AppImage ，snap ，如意玲珑，Android 平台 Android application package

运行的时候需要 LD_LIBRARY_PATH=/opt/swoole/lib swoole-cli -v ，这样来指定这个动态链接库的目录
